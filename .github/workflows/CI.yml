name: CI-Padavan-4.4-V2

on: 
  push:
  workflow_dispatch:

env:
  # МЕНЯЕМ НА РЕПОЗИТОРИЙ С ЯДРОМ 4.4
  src_git: https://github.com/hanwckf/padavan-4.4.git
  build_dir: /opt/rt-n56u
  images_dir: /opt/images
  # Настройки функционала (оставил как в оригинале)
  ENABLED_CFG: |
    CONFIG_FIRMWARE_INCLUDE_TCPDUMP
    CONFIG_FIRMWARE_INCLUDE_HTOP
    CONFIG_FIRMWARE_INCLUDE_NANO
  DISABLED_CFG: |
    CONFIG_FIRMWARE_INCLUDE_SHADOWSOCKS
    CONFIG_FIRMWARE_INCLUDE_WIREGUARD

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    env:
      build_variant: ${{ matrix.build_variant }}
      targets: ${{ matrix.targets }}
    strategy:
      matrix:
        include:
          # ДОБАВЛЯЕМ СЕКЦИЮ ДЛЯ ТВОЕГО РОУТЕРА (MT7628)
          - build_variant: "mt7628"
            targets: "MI-4A_V2"
    steps:
      - uses: actions/checkout@v2
      - name: Prepare environment
        run: |
          sudo apt update
          sudo apt install libtool-bin gperf python-docutils autopoint gettext zip 7z
          mkdir -p $images_dir
      - name: Clone source
        run: |
          git clone --depth=1 $src_git $build_dir
          cd $build_dir
          echo "build_date=$(date +%Y%m%d)" >> $GITHUB_ENV
          echo "image_suffix=$(git rev-parse --short=7 HEAD)_$(date +%Y%m%d)" >> $GITHUB_ENV
      - name: Prepare toolchain
        run: |
          cd $build_dir/toolchain-mipsel
          sh dl_toolchain.sh
      - name: Modify configs for V2
        run: |
          cd $build_dir/trunk/configs/templates
          # Трюк: создаем конфиг MI-4A_V2 на основе обычного MI-4A
          # Если файла MI-4A.config нет в ветке 4.4, используем любой на mt7628
          if [ -f MI-4A.config ]; then
            cp MI-4A.config MI-4A_V2.config
          else
            cp MI-NANO.config MI-4A_V2.config
          fi
          
          # ПРИНУДИТЕЛЬНО СТАВИМ ДРАЙВЕР MT7613 ДЛЯ 5ГГц
          sed -i 's/CONFIG_FIRMWARE_WIFI5_MT7612E=y/CONFIG_FIRMWARE_WIFI5_MT7613=y/g' MI-4A_V2.config
          sed -i '/CONFIG_FIRMWARE_WIFI5_MT7612E/d' MI-4A_V2.config
          echo "CONFIG_FIRMWARE_WIFI5_MT7613=y" >> MI-4A_V2.config
          
          # Применяем твои включенные/выключенные функции
          for n in $ENABLED_CFG; do sed -i "/${n}/c${n}=y" MI-4A_V2.config; done
          for n in $DISABLED_CFG; do sed -i "/${n}/c${n}=n" MI-4A_V2.config; done
      - name: Start build
        run: |
          cd $build_dir/trunk
          # Собираем MI-4A_V2
          fakeroot ./build_firmware_modify MI-4A_V2
          cp -f images/*.trx $images_dir/MI-4A_V2.trx
      - name: Create archive
        if: ${{ success() }}
        run: |
          cd $images_dir
          zip MI-4A_V2_${{ env.image_suffix }}.zip MI-4A_V2.trx
      - name: Upload images to Artifact
        if: ${{ success() }}
        uses: actions/upload-artifact@v2
        with:
          name: Padavan_V2_4.4_${{ env.image_suffix }}
          path: ${{ env.images_dir }}/*.zip
