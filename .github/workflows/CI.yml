name: CI-Padavan-V2-Final

on: 
  push:
  workflow_dispatch:

env:
  # МЕНЯЕМ НА СТАБИЛЬНЫЙ РЕПОЗИТОРИЙ ДЛЯ MT7628
  src_git: https://github.com/hanwckf/rt-n56u.git
  build_dir: /opt/rt-n56u
  images_dir: /opt/images
  ENABLED_CFG: |
    CONFIG_FIRMWARE_INCLUDE_TCPDUMP
    CONFIG_FIRMWARE_INCLUDE_HTOP
    CONFIG_FIRMWARE_INCLUDE_NANO
    CONFIG_FIRMWARE_INCLUDE_TTYD
  DISABLED_CFG: |
    CONFIG_FIRMWARE_INCLUDE_SHADOWSOCKS
    CONFIG_FIRMWARE_INCLUDE_WIREGUARD

jobs:
  build:
    name: build
    runs-on: ubuntu-20.04
    strategy:
      matrix:
        include:
          - build_variant: "mt7628"
            targets: "MI-NANO" # Используем MI-NANO как идеальную базу для 100M роутера

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare environment
        run: |
          sudo apt update
          sudo apt install -y libtool-bin gperf python3-docutils autopoint gettext zip p7zip-full build-essential libssl-dev
          mkdir -p $images_dir

      - name: Clone source
        run: |
          git clone --depth=1 $src_git $build_dir
          cd $build_dir
          echo "build_date=$(date +%Y%m%d)" >> $GITHUB_ENV
          echo "image_suffix=$(git rev-parse --short=7 HEAD)_$(date +%Y%m%d)" >> $GITHUB_ENV

      - name: Prepare toolchain
        run: |
          cd $build_dir/toolchain-mipsel
          sh dl_toolchain.sh

      - name: Modify configs for Mi 4A V2
        run: |
          cd $build_dir/trunk/configs/templates
          
          # Берем MI-NANO.config за основу (он идеально подходит для MT7628)
          cp MI-NANO.config MI-4A_V2.config
          
          # Включаем драйвер MT7613 (критично для 5ГГц на v2)
          sed -i 's/CONFIG_FIRMWARE_WIFI5_MT7612E=y/CONFIG_FIRMWARE_WIFI5_MT7613=y/g' MI-4A_V2.config
          
          # Если строки про MT7613 вообще нет, добавляем её
          if ! grep -q "CONFIG_FIRMWARE_WIFI5_MT7613" MI-4A_V2.config; then
            echo "CONFIG_FIRMWARE_WIFI5_MT7613=y" >> MI-4A_V2.config
          fi
          
          # Применяем твои функции
          for n in $ENABLED_CFG; do sed -i "/${n}/c${n}=y" MI-4A_V2.config; done
          for n in $DISABLED_CFG; do sed -i "/${n}/c${n}=n" MI-4A_V2.config; done

      - name: Start build
        run: |
          cd $build_dir/trunk
          # В ветке 3.4 используется скрипт build_firmware_ci или build_firmware_modify
          fakeroot ./build_firmware_modify MI-4A_V2
          cp -f images/*.trx $images_dir/MI-4A_V2.trx

      - name: Create archive
        if: ${{ success() }}
        run: |
          cd $images_dir
          zip -j MI-4A_V2_firmware_${{ env.image_suffix }}.zip MI-4A_V2.trx

      - name: Upload images to Artifact
        if: ${{ success() }}
        uses: actions/upload-artifact@v4
        with:
          name: Padavan_Mi4A_V2_${{ env.image_suffix }}
          path: ${{ env.images_dir }}/*.zip
          retention-days: 7
